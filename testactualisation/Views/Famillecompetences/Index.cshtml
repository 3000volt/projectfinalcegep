@model IEnumerable<testactualisation.Appdata.Famillecompetence>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomFamille)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomFamille)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idfamille">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idfamille">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idfamille">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
